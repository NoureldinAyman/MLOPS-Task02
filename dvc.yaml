stages:
  preprocess:
    cmd: python preprocess.py
    deps:
      - preprocess.py
      - data/cleaned_merged_heart_dataset.csv
    outs:
      - data/train.csv
      - data/test.csv

  train:
    cmd: python train.py
    deps:
      - train.py
      - data/train.csv
    outs:
      - model.joblib

  validate:
    cmd: python validate.py
    deps:
      - validate.py
      - data/test.csv
      - model.joblib
    metrics:
      - metrics.json:
          cache: false
    plots:
      - confusion_matrix.png:
          cache: false
